<nav class="navbar navbar-default navbar-fixed-top <%= if user_signed_in? then "navbar-logged" end %>" role="navigation">
  <div class='container'>
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-nav-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if user_signed_in? %>
        <%= link_to root_path do %>
          <%= image_tag "merit-without-background.png", class:"logo" %>
        <% end %>
      <% end %>
    </div>
    <div class="collapse navbar-collapse" id="main-nav-collapse">
      <ul class="nav navbar-nav navbar-right nav-links">
        <% if user_signed_in? %>
          <li><%= link_to 'FAQ', about_path %></li>
          <li><%= link_to 'FEEDBACK', new_contact_path, "data-toggle" => "modal", "data-target" => "#myFeedbackModal", remote: true %></li>
          <li class="list-unstyled dropdown">
            <a class="dropdown-toggle profile-dropdown" data-toggle="dropdown">
              <%= image_tag "blank-profile-picture.png", class:"avatar-pic" %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-right">
              <li class="text-center">
                <%= link_to "Settings", edit_user_registration_path(current_user) %>
              </li>
              <li class="text-center">
                <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>